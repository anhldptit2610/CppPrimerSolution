int* ip, &r = ip;       // ip is a pointer to an int, r is a reference to an int
int i, *ip = 0;         // i is an int, ip is an pointer to an int
int *ip, ip2;           // ip is a pointer to an int, ip2 is a int